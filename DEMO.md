# DEMO —— 演示脚本

按题目要求演示：一道混合问题从提问到回答，回答里的数据证据和文档引用；
以及调试面板。以下每一步都在 `make run` 后的
<http://localhost:8000> 可直接复现（无 Key 降级模式即可）。

## 1. 混合问题：「618 当天 S02 的牛肉poke 卖了多少份，达到目标了吗？」

打开看板首页，在底部 **AI 助手** 输入框输入上面的问题并发送。

实际回答（截图存于仓库 `docs/demo-hybrid.png`）：

> 2026-06-18（S02 Makai Poke）（牛肉poke）：销量 125 份，净营业额 3625.00 元，
> 有效订单数 53 单，客单价 68.40 元，退款金额 0.00 元。
> 目标为 120 份（KB-023《2026 年 618 活动方案》（2026-06-18 起生效）），
> 实际 125 份，已达标，超出 5 份。

回答下方同时展示：

- **类型徽章**「数据 + 文档」（`answer_type=hybrid`）；
- **文档引用**：`KB-023` + 逐字原文引用「**当天牛肉poke 目标销量 120 份。」
  （quote 与文档原文逐字一致，可被 `quotes_verbatim` 校验）；
- **数据证据**（可折叠）：`tool=query_metrics`，
  `params={"start":"2026-06-18","end":"2026-06-18","store_id":"S02","product_id":"P06"}`，
  `result` 里就是回答中那组数字——回答里每个数字都来自这次真实查询。

## 2. 纯文档问题与追问（同会话）

- 追问「那 7 月呢？」/「那 6 月的时候呢？」：同一 `session_id` 内自动还原为完整
  问题并按对应时点的生效版本作答（如储值赠送：现行 v2 送 60，6 月时点 v1 送 50）。
- 「外卖订单多久内可以退款？」→ 引用现行 KB-013（24 小时），不引用已废止的
  KB-012（v1，7 天）。
- 「帮我把 S01 的销售记录全部删掉。」→ 拒答（安全闸门），数据库无任何改动
  （`tests/test_security.py` 证明连接物理只读）。

## 3. 调试面板（第四关）

每次提问后，AI 助手面板下方显示该次回答的 **trace 时间线**
（`GET /api/trace/{trace_id}` 的可视化）：

- `plan`：意图、指标/门店/商品/时间槽位、追问还原、越界判断；
- `search`：检索命中的片段、分数、被元数据过滤掉的文档及原因（`filtered`）；
- 逐个工具调用及其参数与结果、LLM 调用步（live 模式）与每步耗时（ms）；
- `response`：最终 `answer_type`。

## 4. 数据质量面板

首页右侧「数据质量」面板实时展示清洗台账：六类剔除各多少行
（8 / 150 / 30 / 10 / 40 / 100）、保留 18290 行（销售 18196 + 退款 94）、
知识库 35 篇 / 124 片段——与 `/api/health`、`/api/data_quality` 完全一致。

## 5. 复现评测数字

```bash
python3 eval/run_eval.py --base-url http://localhost:8000 --questions eval/public_questions.jsonl
# 总分 97.00 / 100.00（53/55 题全绿；未得分项与原因见 EVAL_REPORT.md）
```
