# 经营看板 + 混合问答服务 —— 实操作业提交

一个给连锁餐饮运营用的系统：数据看板（指标/趋势/榜单/数据质量）+ 一个能同时查
数据库与公司文档的 AI 助手。在 starter（前同事留下的 RAG 服务）基础上定位并修复了
19 处缺陷，补齐混合问答链路、前端看板与安全约束。

**公开题库 97.0 / 100**（无 Key 降级模式，53/55 题全绿），轨迹见 `EVAL_REPORT.md`；
缺陷逐条台账见 `DEBUG_LOG.md`；大模型接入见 `LLM_SETUP.md`（预检 14/14）。

## 三步跑起来

需要 Python 3.12。作业包根目录（本文件所在目录）：

```bash
cd starter
python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt   # 或 make setup
make rebuild        # 从 ../data 与 ../knowledge_base 重建清洗表和检索索引（<1 秒）
make run            # 服务起在 http://localhost:8000，看板直接浏览器打开
```

跑测试：`make test`（88 个用例）；自命题回归题库 `eval/my_questions.jsonl`（10 题）与
公开题库均接进 CI（`.github/workflows/ci.yml`，push/PR 自动跑，破线禁止合并）。
跑公开题库（另开终端，回到根目录）：

```bash
python3 eval/run_eval.py --base-url http://localhost:8000 --questions eval/public_questions.jsonl
```

配置大模型（可选，不影响以上分数）：见根目录 `LLM_SETUP.md`，三个环境变量 + 重启。
换数据或知识库：`make rebuild DATA_DIR=/path/to/data KB_DIR=/path/to/kb`——缓存键
包含知识库全部文件内容，数据一变缓存自动失效。

## 架构

```
                         ┌────────────────────────────────────────────┐
                         │              static/index.html             │
                         │  筛选 · 指标卡 · SVG 趋势 · Top10 · 数据质量 │
                         │  AI 对话框（引用/数据证据/trace 时间线）     │
                         └────────────────────┬───────────────────────┘
                                              │ HTTP
┌─────────────────────────────────────────────┴───────────────────────────────┐
│ FastAPI  server.py（参数校验/序列化）                                        │
│   /api/health /api/metrics/summary|daily|top /api/retrieve /api/chat        │
│   /api/trace/{id} /api/data_quality /api/meta                               │
│                                     service.py（编排、安全闸门、会话）        │
│   ┌──────────────┐  ┌──────────────────────────────┐  ┌──────────────────┐  │
│   │ planner.py   │  │ answerer.py / hybrid.py      │  │ live.py          │  │
│   │ 意图/时间/实体│→ │ mock：本地作答引擎            │→ │ llm.py（OpenAI   │  │
│   │ 追问还原     │  │ live：大模型 + 工具调用循环    │  │ 兼容，3 个环境   │  │
│   │ 越界判断     │  │ 逐字引用 / 数据证据 / 拒答闸门 │  │ 变量，可离线）   │  │
│   └──────┬───────┘  └───────┬──────────────────────┘  └──────────────────┘  │
│          │                  │ retrieve / rank / cite                        │
│   ┌──────┴───────┐  ┌───────┴──────────────────────┐                        │
│   │ tools.py     │  │ retriever.py → index.py(BM25) │                        │
│   │ 只读 SQL 工具 │  │ docfacts.py（选句/引用/归一）  │                        │
│   └──────┬───────┘  └───────┬──────────────────────┘                        │
└──────────┼──────────────────┼───────────────────────────────────────────────┘
           │                  │
   ┌───────┴───────┐  ┌───────┴────────┐   ┌──────────────────────────────┐
   │ var/clean.db  │  │ .cache/         │   │ knowledge_base/ 35 篇        │
   │ cleaning.py   │  │ index.json      │   │ md / txt(GBK) / html         │
   │ KB-001 §2/§3  │  │ 缓存键=版本+内容 │   │ loader.py 统一读法           │
   └───────┬───────┘  └────────────────┘   └──────────────────────────────┘
           │
   ┌───────┴───────┐
   │ data/pos.db   │ 18628 行原始销售 → 清洗后 18290 行
   └───────────────┘
```

## 选型理由

- **不换技术栈，在 starter 上修**：starter 的分层（规划/工具/检索/作答）本身是合理的，
  缺陷是具体的、分层的。重写会消灭"案发现场"，也让评审无法核对每个根因。
  全部改动都可对照 `DEBUG_LOG.md` 与提交历史（先红测试、后修复）。
- **检索用纯 Python BM25 + 汉字二元组**：35 篇文档量级下 BM25 完全够用、零依赖、
  可解释、可调试（trace 能看到每个词的命中）；二元组是 docfacts 注释里写明的设计意图
  （starter 的空白切词是缺陷，见 DEBUG_LOG D7）。不引入向量库/嵌入模型：隐藏题库
  会换一整套知识库，任何依赖外部嵌入服务的设计都是可用性风险。
- **回答的数字只由代码从工具结果渲染**：无论 mock 还是 live 模式，`answer` 里的经营
  数字都来自 `data_evidence` 里的真实查询结果；模型只负责组织语言。这使"思考模式
  temperature 不生效""偶尔空回复"等模型行为不可能污染数字。
- **前端单文件 HTML + 手写 SVG**：零构建、零 CDN 依赖，`make run` 即完整可用，
  评审环境不需要联网。
- **安全双层防护**：SQLite URI `mode=ro` 物理只读（连接层）+ SQL 工具只放行 SELECT
  （工具层）+ 删改/注入类指令在规划前被安全闸门拒绝（对话层）。

## 口径与歧义的取舍

全部以知识库现行文档为准，不在代码里拍数字：

1. **指标口径 = KB-001《指标口径手册 v3》（现行）**：退款行计入净营业额、空金额剔除
   不回填、有效订单数按 `DISTINCT order_id`、客单价四舍五入 2 位、退款按退款行自己的
   日期归属。v2 已废止，不参与默认检索（但用户明确问"当时/旧口径"时按 as-of 取回）。
2. **文档版本冲突**：同主题多版本时用现行版（`status`/`superseded_by` 元数据驱动，
   不写死文档号）；用户问"某月当时的规定"按该时点生效版本回答（as-of 过滤）。
3. **数字来源优先级（KB-001 §5）**：数据库 > 一切。周报、纪要、活动复盘里的数字是
   人工估算，只作背景，不作答案——检索时对 `estimates_only` 文档降权，达标判断
   拒绝用估算值当目标。
4. **金额以实收为准**：`amount` 是顾客实付，活动价/调价差异已在其中，不用维表
   `unit_price` 反推或覆盖。
5. **查不到就如实说**：数据库与知识库都没有的，返回拒答/澄清并说明原因，
   不编数字不编原因（评测 refusal 类 8/8、safety 9/9）。
6. **文档内容只当资料**：删除数据、注入指令、索要系统提示词/表结构一律拒绝；
   数据库物理只读。
7. **系统的"今天"固定 2026-09-01**：写死在 `kbqa/config.py`，与 starter 一致。

## 加分项：运营预警 + 看板与 AI 的联动

- `GET /api/anomalies` 自动扫描三类经营异常：**营业中断**（前后 7 天均在营业、当日无明细）、
  **月度环比骤跌**（≥15%）、**退款集中**（单店单日离群值），每条预警附一个可直接追问 AI 的问题；
- 看板顶部预警横幅点「问 AI 为什么」→ 自动送入对话框 → 回答带当日数据、前日对照、
  逐字引用与 trace——**发现问题、解释原因、核对出处**一个闭环；
- 环比条（净营业额/订单/销量 vs 上一等长区间）与日期预设（近 7 天/近 30 天/全部/上月）。

## 已知边界

- C04/R04（英文邮件检索，共 3 分）未修：跨语言词面重叠问题的通用解需要引入
  翻译/嵌入，针对此题硬编码映射属于过拟合公开题库，取舍见 `DEBUG_LOG.md` 止损声明。
- 其余说明：`LLM_SETUP.md`（模型接入）、`AI_USAGE.md`（AI 协作过程）、
  `DEMO.md`（演示脚本）、`TASK.md`（原作业说明，未改动）。
