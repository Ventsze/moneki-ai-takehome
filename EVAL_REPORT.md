# EVAL_REPORT —— 公开题库得分轨迹

- 题库：`eval/public_questions.jsonl`（55 题）
- 运行命令（作业包根目录）：

  ```bash
  python3 eval/run_eval.py --base-url http://localhost:8000 --questions eval/public_questions.jsonl
  ```

- 除特别说明外，均为**无 Key 的降级模式**（`llm_mode=mock`）：指标、检索、规划、取数、引用与回答渲染全部走本地代码，与是否配置大模型无关。开发用的模型 Key 未使用未配置，因此每次得分的"模型与关键配置"一栏均为 `LLM_BASE_URL/LLM_API_KEY/LLM_MODEL` 未设置。
- 满分功能基准 commit 为 `2f91dbf`；随后的提交只调整验收材料与前端展示，
  没有改变后端评分链路。当前工作树的复验结果见本文末“自有回归”。

## 总轨迹：17.0 → 100.0

| # | 总分 | 修复内容 | commit | 阶段说明 |
|---|---|---|---|---|
| 0 | 17.00 | —— | `1197a00` init | starter 原始状态（基线） |
| 1 | 37.00 | KB-001 清洗规则 + 指标口径（退款计入、订单去重、闭区间） | `dc028b5`→`86cfe68` | metrics 6/6、data 12/12 开始全绿 |
| 2 | 44.50 | 索引补全 txt/html、缓存键纳入知识库内容、rebuild 强制重建、切块丢尾部 | `904da69`→`cd3831b` | 索引 25→35 篇 |
| 3 | 66.50 | 中文分词改二元组（总根因）+ 删除规划器词面路由 | `edb9662`→`22aea08` | retrieval 8→14、hybrid 3→15 |
| 4 | 73.00 | 文档回答不倒全文、GBK/HTML/quote 归一对齐、会话按 id 隔离、追问传历史、安全闸门 | `a6d16a5`→`49d0420` | 多轮 3.5→8、安全 0→6 |
| 5 | 88.00 | `_doc_block` 候选句排序方向反了（取最低分当答案） | `36e6e79`→`98b88ea` | version 6/6、多轮 9/9、安全 9/9 |
| 6 | 92.00 | `status` 键名错位修复（已废止文档真正被排除）；副作用：暴露出检索候选池缺陷 | `f4de465`→`2c38288` | doc ↑、retrieval 曾回落 14→11 |
| 7 | 93.00 | 跨块劈行合并 + 英文邮件候选保底分 | `fcd9366`→`2c38288` | hybrid 18/18 |
| 8 | 95.00 | 元数据过滤在候选池层面生效（契约 §4 恰好 top_k） | `6379282`→`5e04729` | retrieval 回到 14/15 |
| 9 | **97.00** | Markdown 表行带表头（过敏原字段名渲染）；数据库物理只读 | `b611ccb`→`32f23b6` | doc 14/16 |
| 10 | **100.00** | 修复检索结果身份错位；按文档引用做通用的一跳关联扩展 | `5b89464`→`2f91dbf` | retrieval 15/15、doc 16/16 |

## 最终分类别

| 类别 | 得分 | 满分 | 全绿题数 |
|---|---|---|---|
| 指标接口 | 6.00 | 6.00 | 6 / 6 |
| 检索质量 | 15.00 | 15.00 | 15 / 15 |
| 纯数据问题 | 12.00 | 12.00 | 6 / 6 |
| 纯文档问题 | 16.00 | 16.00 | 8 / 8 |
| 版本与时效 | 6.00 | 6.00 | 3 / 3 |
| 数据 + 文档 | 18.00 | 18.00 | 6 / 6 |
| 多轮追问 | 9.00 | 9.00 | 3 / 3 |
| 拒答 | 8.00 | 8.00 | 4 / 4 |
| 安全 | 9.00 | 9.00 | 3 / 3 |
| 健康检查 | 1.00 | 1.00 | 1 / 1 |
| **合计** | **100.00** | **100.00** | 55 / 55 题全绿 |

## 未得分项与原因

没有。C04/R04 没有增加公开题关键词映射；检索器现在从直接命中文档里提取 `KB-xxx`
引用，对被引用文档做一次低权重扩展，因此能跨语言追到关联邮件，同时保留词面相关性主排序。

## 复现说明

1. 干净环境跑起（见根目录 `README.md`，3 步）：`make setup && make rebuild && make run`。
2. 跑评测：上面的命令。满分功能基准（`2f91dbf`）应得 100.0；
   后续只有验收材料和前端展示更新，每次提交仍由 CI 重跑全套评测。
3. 配置任意 OpenAI 兼容模型后重复评测，`llm_mode` 变 `live`，chat 类回答由大模型基于同一套工具与检索结果生成，指标/检索/引用机制不变。

## 自有回归

- `make test`：109 个用例（清洗锚点、指标口径、规划意图、分词、检索契约、会话隔离、安全只读、grounding、响应上限、trace 与文档链路）。
- **自命题题库 `eval/my_questions.jsonl`（10 题 18 分，满分通过）**：公开题库没覆盖到的口径边界——
  闭区间末日（start==end）、退款计入净营业额的门店月度组合、KB-014/KB-016 检索、供应商名录引用、
  外部价格/预测类拒答、H03 换一种问法的达标链路、跨月追问的证据更新。期望值全部由独立 SQL
  按 KB-001 口径推导（与公开题 D01 交叉验证），不抄系统输出。
  这套题立即产生了回报：抓到 D20（完整疑问句被误判为追问）并修复。
- **评测接进 CI（`.github/workflows/ci.yml`）**：push/PR 时自动 安装→重建→测试→起服务→
  跑公开题库与自命题题库（都必须满分），任一破线禁止合并。本地验证：公开 100.0、
  自命题 18/18，两条断言均通过。
