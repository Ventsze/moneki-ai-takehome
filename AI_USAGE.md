# AI_USAGE —— AI 协作过程说明

按作业要求分三部分：用了哪些 AI 工具怎么拆任务、AI 给过的错误诊断及发现过程、
哪些判断是我自己做的。提交历史（当前 42 个 commit，先红测试后修复）可逐条对照。

## 一、用了哪些 AI 工具，怎么拆任务

- **主力：ZCode（GLM，agentic coding CLI）**，用于初始实现与逐题修复。
- **提交审计：OpenAI Codex**，用于完成后的独立代码审查、攻击性回归测试、浏览器验收与文档复核。
- 拆任务的方式：我只把"目标 + 验收标准"交给 AI，把"什么算修好了"的判定权留在
  手里。真实任务的拆法：

  1. 跑通 starter + 评测，拿到基线 17 分（AI 执行，我核对 report.md 的分类别表）；
  2. 逐关修复：每个缺陷一个循环——AI 提出根因假设 → 我要求"先写会红的测试" →
     commit 红测试 → AI 修复 → 测试转绿 + 重跑评测看分类别变化 → commit 修复；
  3. 每次评测后一起决定下一个进攻方向（按丢分排序，而不是按代码顺序）。

- 真实 prompt 例子（对应 D7 中文分词缺陷，原样摘录）：

  > C02 问过敏原被拒答，notes 说"词表覆盖率 0.00"，但"牛肉poke""过敏原"明明白白在
  > KB-040 里。不要看 vocab_coverage 本身，先从 tokenize 开始查整条链路：问句的
  > token 和文档的 token 到底能不能有交集。找到根因后先写红测试钉死"问句与文档
  > 必须有公共 token"，commit，再修。

  这个 prompt 里我做了两个约束：指定排查起点（tokenizer 而不是闸门阈值）、
  强制先红后绿。前者来自我对症状的判断，后者是本题 Commit 要求。

- AI 承担的部分：读代码定位、写测试与实现、跑评测与整理数字、生成文档初稿
  （本文档与 README/DEBUG_LOG 由 AI 起草，我逐条核对事实后定稿）。

## 二、AI 给过哪些错误的诊断或修复，我是怎么发现的

1. **测试自己写错（两次）**：清洗测试的重复行用例期望值算错（kept_rows 应为 2 而非 1）、
   `__file__` 忘了包 `Path()`。发现方式：修复实现后测试仍红，重读断言发现是测试错
   而不是实现错——修的是测试。教训：红测试先要怀疑自己。
2. **跨块测试构造错误**：TestCrossChunkLine 第一版让测试行恰好没跨过 300 字边界
   （head=296 时边界落在换行前），测试直接绿了，是假绿。发现方式：把断言子串改成
   必须包含跨块接缝（"首月全门店合计目标销量 900 杯"）后测试才红，随后修复才有效。
3. **日历校验带来的 3 行差异**：验证脚本（AI 的快速模拟）没做日历校验，
   `2026-13-45` 被放过、锚点写成 18293；实现里加了 `datetime.date` 校验后锚点差 3 行。
   发现方式：锚点断言失败。最后以"严格剔除才是对 KB-001 的正确执行"拍板，锚点改
   18290，并把 8 行坏日期的构成写进注释。
4. **C04 修复的过度冲动（被自己叫停）**：AI 多次提出"给断供/赔偿加跨语言同义词映射、
   调大 KB-022 的文档先验"就能拿满这 3 分。我拒绝了题目特化。后续审计找到通用解：
   利用文档正文已有的 `KB-xxx` 引用做一次低权重扩展，不绑定词语、文档号或答案数字。
5. **前端全页截图的伪影**：AI 初判"页面内容重复渲染"，重新用视口截图核对后确认是
   fullPage 截图对滚动页面的伪影，页面本身没有问题——没白改一行代码。
6. **preflight 首次全败**：AI 第一次跑 preflight 报 P1/P12 失败（服务收不到注入变量）。
   读 `README_llm_gateway.md` 确认是交互式流程（打印环境变量→等人工重启→回车），
   不是代码问题；用脚本编排后 14/14 通过。教训：工具的"失败"先查用法再查实现。

## 三、哪些判断是我自己做的，为什么不交给 AI

1. **修复顺序与边界**：每轮进攻哪个缺陷（按丢分 ÷ 风险排序），并拒绝用公开题词表、
   文档号白名单或答案数字换分。C04/R04 只在找到通用的文档引用扩展机制后才继续修复。
2. **口径的最终解释权归 KB-001**：AI 多次提出"合理的"业务假设（比如退款要不要计入、
   订单数怎么算），我一律要求先去知识库找成文规定，找到了就以文档为准——本题的
   设计就是"答案在文档里不在代码里"，任何 AI 的业务直觉都不该越过手册。
3. **不写死任何东西的红线**：隐藏题库会换数据换知识库。我给所有实现定了验收标准：
   不出现硬编码的数字、答案、文档内容、映射表；缓存键必须随数据变化。AI 的补丁
   常不自觉往"针对当前题"的方向走，每次都按这条红线拦。
4. **先红后绿的流程本身**：这是题目明示的评分项，也符合我对调试过程可信度的要求。
   AI 倾向直接给修复，我坚持每个缺陷先留下"修复前确实是红的"证据（15 个测试
   commit 在前、修复 commit 在后，历史可查）。
5. **事实核对**：所有文档（含本文）里的数字——得分轨迹、commit 号、清洗台账、
   预检结论——由我对照评测输出与 git log 逐一核对后才提交。AI 写的初稿里有两处
   得分与 commit 对不上，已纠正。
